from setuptools import setup, find_packages

setup(
    name='sum_lib',
    version='0.1',
    description='A simple library for doing sums.',
    author='TEST',
    author_email='',
    packages=find_packages(),
    install_requires=[],
)
